[pytest]
markers = 
    api: mark for API tests
    api1: API critical path mark

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
log_level=INFO    